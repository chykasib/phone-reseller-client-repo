import React from 'react';
import { useTitle } from '../../../Hooks/UseTitle';

const AllUsers = () => {
    useTitle('all users')
    return (
        <div>

        </div>
    );
};

export default AllUsers;